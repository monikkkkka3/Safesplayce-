// Top-level build file — source: github.com/monikkkkka3/Safesplayce-/livestream/android
// The version catalog in gradle/libs.versions.toml keeps dependency versions in one place.
plugins {
    // Uses the plugin aliases defined in gradle/libs.versions.toml so versions stay consistent.
    alias(libs.plugins.android.application) apply false
    alias(libs.plugins.kotlin.android) apply false
}
